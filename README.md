# mcp-client-server

Monorepo containing MCP client and server implementations.

## Projects
- `mcp_client` – MCP client implementation
- `terminal_server` – MCP terminal-based server

Each project is self-contained with its own dependencies.
